"""CopilotKit runtime endpoint using AG-UI protocol."""

from fastapi import APIRouter

router = APIRouter()

# The actual endpoint is added in main.py using ag_ui_adk.add_adk_fastapi_endpoint
# This module is for any additional CopilotKit-related endpoints
